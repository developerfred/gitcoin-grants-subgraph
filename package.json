{
  "name": "gitcoin-grants-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ developerfred/gitcoin-grants-subgraph",
    "create-local": "graph create --node http://localhost:8020/ developerfred/gitcoin-grants-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ developerfred/gitcoin-grants-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 developerfred/gitcoin-grants-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.19.0",
    "@graphprotocol/graph-ts": "^0.19.0",
    "node-forge": "~0.10.0",
    "yargs-parser": "^20.2.4"
  }
}
